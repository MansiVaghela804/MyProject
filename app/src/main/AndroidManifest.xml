<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.myproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ExternalStorage.ExternalStorageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InternalStorage.InternalStorageActivity"></activity>
        <activity android:name=".BMICalculator.ResultActivity" />
        <activity android:name=".BMICalculator.StartActivity" />
        <activity android:name=".RoomDatabase.RoomActivity" />
        <activity android:name=".Listview.MainActivity" />
        <activity
            android:name=".Navigationdrawer.NavigationActivity"
            android:label="@string/title_activity_navigation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Time_Date_Picker.Time_picker" />
        <activity android:name=".Intent.First_Activity" />
        <activity android:name=".Intent.Second_Activity" />
        <activity android:name=".RatingBar.RatingActivity" />
        <activity android:name=".SharedPreferences.Data_store_activity" />
        <activity android:name=".SharedPreferences.LoadActivity" />
        <activity android:name=".Counter.CounterActivity" />
        <activity android:name=".WebView.WebActivity" />
        <activity android:name=".Toolbar.Toolbaractivity" />
        <activity android:name=".AnimatedSplashscreen.MoveActivity" />
        <activity android:name=".Fragment.FragmentActivity" />
        <activity android:name=".Alertdialog.AlertActivity" />
        <activity android:name=".FirstLec.FirstActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".GoogleMap.MapsActivity"
            android:label="@string/title_activity_maps" />

        <service
            android:name=".GoogleMap.GoogleService"
            android:enabled="true" />

        <activity android:name=".Tabs.TabActivity" />
        <activity android:name=".FirstLec.CustomToast" />
        <activity android:name=".UI_Widgets.SwitchActivity" />
        <activity android:name=".UI_Widgets.Popup_Menu" />
        <activity android:name=".Listview.Listview_one" />
        <activity android:name=".Alertdialog.Main2Activity" />
        <activity android:name=".Recycleview.RecyclerActivity" />
        <activity android:name=".ContactUs.ContactusActivity" />
        <activity android:name=".RegistrationApi.RegistrationActivity" />
        <activity android:name=".UI_Widgets.Splashscreen" />
        <activity android:name=".changepassword.PasswordActivity" />
        <activity android:name=".SearchView.SearchActivity" />
        <activity android:name=".ExpandableRecycleview.ListExpandActivity" />
        <activity android:name=".SignIn.SignInActivity" />
        <activity
            android:name=".Navigationdrawer.BottomNavigationActivity"
            android:label="@string/title_activity_bottom_navigation" />
        <activity
            android:name=".GoogleMap.MapsActivity2"
            android:label="@string/title_activity_maps2" />
        <activity android:name=".LoadImage.ImageGalleryActivity" />
    </application>

</manifest>